<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ shop.name }} - {{ page_title }}</title>
  
  {{ content_for_header }} <!-- This loads Shopify's required scripts -->
  
  <!-- Load your CSS -->
  {{ 'theme.css' | asset_url | stylesheet_tag }}
  
  <!-- Load your JavaScript -->
  {{ 'theme.js' | asset_url | script_tag }}
</head>
<body>
  <!-- Include your header -->
  {% render 'header' %}
  
  <main>
    <!-- This is where page content will be injected -->
    {{ content_for_layout }}
  </main>
  
  <!-- Include your footer -->
  {% render 'footer' %}
</body>
</html>